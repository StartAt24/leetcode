#include <vector>
using namespace std;

class Solution {
public:
    int minSubArrayLen(int target, vector<int>& nums) {
        int left = 0, right = 0;
        vector<int> window;
        
        while (right < nums.size()) {

        }
    }
};