#include <string>
#include <vector>
using namespace std;

int slide(vector<string> s) {
    int slow = 0, fast = 0;
    vector<string> window;
    while(slow < fast) {
        window.push_back(s[fast]);
        fast++;
        while(xxx) {
            windows.pop_front();
            slow++;
        }
    }

}