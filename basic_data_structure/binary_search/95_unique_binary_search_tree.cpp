#include "BTree.h"
#include <vector>
using namespace std;

class Solution {
public:
    vector<TreeNode*> generateTrees(int n) {
        // 假设这个数是 [1,2]
        // 则以1为root 就一种情况，以2为root 也是一种情况
        // lo = 1, hi = 2;
        i == 1
        left = count(1, 0) = 1;
        right = count(2,2);
        // lo = 2, hi = 2;
        right = count (2, 1)
              = count (3, 2);



    }
};
