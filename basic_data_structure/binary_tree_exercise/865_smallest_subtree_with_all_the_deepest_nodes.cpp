#include "BTree.h"

class Solution {
public:
    TreeNode* subtreeWithAllDeepest(TreeNode* root) {
        
    }
private:
    int depth(TreeNode* root) {
        if (!root)
            return 0;
        
            
    }
};