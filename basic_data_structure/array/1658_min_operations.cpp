#include "common.hpp"
class Solution {
public:
    int minOperations(vector<int>& nums, int x) {
        // 反过来思考，寻找最长连续子数组
        // 使得 最长连续子数组的和为 nums.sum() - x;
        int n = nums.size();
        for(int i = 0;)
    }
};