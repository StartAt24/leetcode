#include <string>
using std::string;
class Solution {
public:
    int strStr(string haystack, string needle) {
        char begin = haystack[0];
        int ret = -1;
        for (int i = 0; i < needle.size(); i++) {
            if (needle[i] == begin) {
                
            }
        }
        return ret;
    }
};