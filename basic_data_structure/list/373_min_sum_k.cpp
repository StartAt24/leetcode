#include "common.hpp"
class Solution {
public:
    vector<vector<int>> kSmallestPairs(vector<int>& nums1, vector<int>& nums2, int k) {
        int p1 = 0;
        int p2 = 0;
        // 1, 7, 11
        // 2, 4, 6

        vector<vector<int>> res;
        for (int i = 0; i < k; i ++) {
            
        }
        return res; 
    }
private:

};